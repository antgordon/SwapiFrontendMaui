<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiApp1.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiApp1"
    xmlns:view="clr-namespace:MauiApp1.View"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
        <ShellContent
        Title="Films"
        ContentTemplate="{DataTemplate view:FilmsPage}"
        Route="Films" />
        <ShellContent
        Title="Planets"
        ContentTemplate="{DataTemplate view:PlanetSearchPage}"
        Route="Planets" />
        <ShellContent
        Title="People"
        ContentTemplate="{DataTemplate view:PeopleSearchPage}"
        Route="People" />
        <ShellContent
        Title="Starships"
        ContentTemplate="{DataTemplate view:StarshipsSearchPage}"
        Route="Starships" />
        <ShellContent
        Title="Species"
        ContentTemplate="{DataTemplate view:SpeciesSearchPage}"
        Route="Species" />
        <ShellContent
        Title="Vehicles"
        ContentTemplate="{DataTemplate view:VehiclesSearchPage}"
        Route="Vehicles" />
        <ShellContent
        Title="AboutBinding"
        ContentTemplate="{DataTemplate view:AboutBinding}"
        Route="AboutBinding" />

    </TabBar>
</Shell>
